# チューニングログ

## 記録方針

一度に複数の挙動を変えすぎず、変更内容、試験条件、観察結果、採用 / 戻し判断を残す。意味のあるチューニングを行った後は、新しいパラメータファイルを保存する。

チューニング前ベースライン:

```text
params/tuned/20260610_before_tune.param
```

現在の作業中パラメータ:

```text
params/tuned/20260612_01_after_lidar_simple_avoid_acro_margin1_stop_check.param
```

## 現在の状態

- FC換装: 済み
- 屋内Manual低速走行: 済み
- 屋外チューニング: 一部実施
- Acro割当: 済み
- LiDAR室内確認: 済み
- Speed / Throttle controller調整: QuickTune前
- Turn Rate controller調整: QuickTune前
- Navigation controller調整: 未実施
- Rover QuikTune: 未実施

## チューニング前に必ず記録するもの

- [ ] `params/tuned/20260610_before_tune.param` からの変更差分
- [ ] `COMPASS_ENABLE=1`へ戻したか
- [ ] GPS/Compassマスト長変更後の実測長さ、固定状態、向き
- [ ] Compass Calibration前後のパラメータ保存
- [ ] Mission Planner Compass画面の表示名、External、Orientation、Use Compass状態
- [ ] Compass Calibration後のPreArm / EKF / GPS / Compass結果
- [ ] Battery実測電圧とMission Planner表示
- [ ] Battery failsafe値、または低電圧時の手動中止基準
- [ ] `MODE_CH=5` の実スイッチ位置
- [ ] Manual / Hold / Acro の割当
- [ ] `RC7_OPTION=153` の実機スイッチと機能
- [ ] RC failsafe確認結果
- [ ] Mission Planner再起動時に `Radio failsafe on` が出ていないこと
- [ ] `RNGFND1_MAX_CM=700` を維持するか変更するか
- [ ] 走行ログ / `.BIN` / `.tlog` の外部保存先

## チューニング記録

| 日付 | 対象 | 変更内容 | 試験条件 | 結果 | 採用判断 | 次の対応 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-06-10 | ベースライン | `params/tuned/20260610_before_tune.param` をチューニング前正本に設定 | 屋外前 | 未走行 | 採用 | 屋外前安全確認へ |
| 2026-06-11 | タイヤ浮かせ安全確認 | パラメータ変更なし。Manual / Hold / Disarm / RC failsafeを確認 | タイヤ浮かせ状態 | 1から7まで確認済み | 通過 | 屋外静置でCompass / GPS / EKF / Battery / LiDAR確認へ |
| 2026-06-12 | LiDAR Simple Object Avoidance | `OA_TYPE=0`、`PRX1_TYPE=4`、`AVOID_ENABLE=7`、`AVOID_MARGIN=1.0`でAcro停止確認 | 屋内 / 近距離、Acro | `THR.DesSpeed` が0へ低下し、`RCOU.C3` が中立付近へ復帰 | 採用 | 屋外で `AVOID_MARGIN=2.0` の再現確認 |

## 次に行うチューニング順

LiDARの室内確認は完了扱いにする。QuickTuneを早く行うため、LiDAR / Object Avoidanceの深掘りは保留し、QuickTune前提チェックへ進む。

1. QuickTune前安全確認
   - Compass / GPS / EKF / Battery / RC failsafe
   - Manual / Hold退避
   - 走行場所と停止手順
2. QuickTune準備
   - Lua Scripts有効化
   - `rover-quicktune.lua` をSDカードへ配置
   - `RTUN_ENABLE=1`
   - 開始 / 中断用のAUX操作を決める
   - Circle modeへ入る方法を決める
3. QuickTune地上確認
   - MessagesにQuikTune出力が出ること
   - Low / Midの開始・停止操作
   - 中断手順
4. QuickTune屋外実行
   - 広い場所、GPS良好、Circle mode
   - 振動や発振が出たら即Low / Hold / Manual / Disarm
5. QuickTune後確認
   - 保存された `ATC_STR_RAT_*`、`CRUISE_*`、`ATC_SPEED_*` を記録
   - Manual / Acro / Guided / Autoを低速で再確認
6. LiDAR / Object Avoidance再確認
   - QuickTune後に必要なら `PRX1_TYPE=4`、Proximity Viewer、PRXログを再確認

## パラメータ変更テンプレート

### YYYY-MM-DD 対象

- 目的:
- 場所:
- 路面:
- 天候:
- 操作者:
- 開始時パラメータ:
- 終了時パラメータ:
- ログ保存先:

#### 変更内容

| パラメータ | 変更前 | 変更後 | 理由 | 結果 |
| --- | --- | --- | --- | --- |
| | | | | |

#### 観察

- ステアリング:
- スロットル:
- 速度維持:
- 旋回挙動:
- 停止 / Hold:
- Battery:
- GPS / Compass / EKF:
  - マスト長:
  - Compass Calibration前param:
  - Compass Calibration後param:
  - Compass表示名 / External / Orientation:
  - PreArm結果:
- LiDAR / Auto-stop:
- フェイルセーフ / 安全:

#### 判断

- 採用 / 戻す / 再試験:
- 戻したパラメータ:
- 保存した `.param`:
- 次の作業:

## 重点パラメータ

### 安全 / 入口条件

| パラメータ | 現在値 | 次の判断 |
| --- | --- | --- |
| `COMPASS_ENABLE` | `0` | 屋外試験前に`1`へ戻す |
| `BATT_LOW_VOLT` / `BATT_CRT_VOLT` | `0` / `0` | 低電圧時の中止基準を決める |
| `BATT_FS_LOW_ACT` / `BATT_FS_CRT_ACT` | `0` / `0` | 初期試験は手動監視。運用前に再検討 |
| `MODE_CH` | `5` | 実スイッチ位置を記録 |
| `MODE1/2/5/6` | `4` | Hold。重複位置をAcro一時割当に使える |
| `MODE3/4` | `0` | Manual。退避先として残す |
| `RC7_OPTION` | `153` | 意味が確定するまで変更しない |

### Speed / Throttle

| パラメータ | 現在値 | 次の判断 |
| --- | --- | --- |
| `CRUISE_SPEED` | `2` | 初回屋外で速ければ下げる |
| `CRUISE_THROTTLE` | `50` | 実走行で再学習または維持 |
| `ATC_SPEED_P` / `I` / `D` / `FF` | `0.2` / `0.2` / `0` / `0` | まずログで追従確認 |
| `ATC_ACCEL_MAX` / `ATC_DECEL_MAX` | `1` / `0` | 発進 / 停止距離で判断 |

### Turn Rate

| パラメータ | 現在値 | 次の判断 |
| --- | --- | --- |
| `ACRO_TURN_RATE` | `180` | 実測最大旋回レートより少し低く設定 |
| `ATC_STR_RAT_FF` / `P` / `I` / `D` | `0.2` / `0.2` / `0.2` / `0` | まずログ取得。必要時のみ変更 |
| `ATC_STR_RAT_MAX` | `120` | `ACRO_TURN_RATE` と整合確認 |
| `ATC_TURN_MAX_G` | `0.6` | 横滑り / 転倒リスクで判断 |

## 2026-06-11 Roverチューニングサマリ

詳細記録:

```text
logs/test_runs/20260611_rover_tuning_summary.md
```

保存済みパラメータ / ログ:

```text
params/tuned/20260611_01_after_acro_assignment.param
params/tuned/20260611_03_acro_low_speed_check.param
params/tuned/20260611_07_after_lidar_oa_bendyruler_setup.param.param
logs/accepted/20260611_03_acro_low_speed_check.md.bin
logs/accepted/20260611_04_acro_s_curve_turnrate_check.bin
logs/accepted/20260611_05_navigation_low_speed_straight_check.bin
logs/accepted/20260611_06_lidar_static_distance_check.bin
```

通過扱い:

- Acro割当
- Manual / Hold退避
- Acro低速走行
- Acro S字TurnRate
- Guided低速移動
- Auto Mission / Waypoint
- Waypoint到達: WP1-WP4
- Mission Complete
- LiDAR静置距離確認

制御系判断:

- Speed controllerは良好。`ATC_SPEED_*` は現状変更不要。
- Turn Rate controllerは左右差が小さく、初回低速として良好。`ATC_STR_RAT_*` は現状変更不要。

LiDAR / Object Avoidance判断:

- LiDARは静置では良好に距離追従。
- 走行ログでは0mが多いため、障害物停止には追加確認が必要。
- `OA_TYPE=1` でMission Plannerの `RangeFinder1(cm)` が `0.00` になり、`OA_TYPE=0` に戻すと距離表示が復活。
- BendyRulerは保留し、まず `OA_TYPE=0` のSimple Object Avoidanceで確認する。

残課題:

- 終了後に `PreArm: Check mag field (xy diff:117>100)` が出たため、次回ARM前にCompass確認。
- Simple Object Avoidance未実施。
- BendyRuler未実施。
- RTL / SmartRTL未実施。
- Battery failsafeは長時間走行前に設定検討。

次の最優先:

```text
20260612_01_lidar_simple_avoid_acro_stop_check
```

## 2026-06-12 LiDAR Simple Object Avoidance確認

詳細記録:

```text
logs/test_runs/20260612_LiDAR設定_簡易障害物回避_Acro停止確認.md
```

保存済みパラメータ:

```text
params/tuned/20260612_01_after_lidar_simple_avoid_acro_margin1_stop_check.param
```

判断:

- `OA_TYPE=0` のSimple Object Avoidanceで、Acro中のLiDAR障害物停止が有効に働くことを確認。
- `AVOID_MARGIN=1.0` はブレーキ開始距離ではなく、残したい停止余白として扱う。
- `RFND.Dist` と `PRX.D0` が記録され、RangeFinderからProximityへの変換を確認。
- `THR.DesSpeed` が0へ低下し、その後 `RCOU.C3` が中立付近へ戻ったため、採用扱い。
- 次回は屋外で `AVOID_MARGIN=2.0` の再現確認を行う。

推奨設定:

```text
OA_TYPE          = 0
PRX1_TYPE        = 4
AVOID_ENABLE     = 7
AVOID_MARGIN     = 2.0
AVOID_BACKUP_SPD = 0
RNGFND1_MAX_CM   = 700
```

## 2026-06-12 LiDAR Simple Object Avoidance確認結果

詳細記録:

```text
logs/test_runs/20260612_01_lidar_simple_avoid_acro_stop_check.md
```

結果:

```text
AcroModeでも2mで止まらない
```

判断:

- Simple Object AvoidanceのAcro実走確認は未通過。
- Rover公式上はSimple Object AvoidanceはManual以外で停止を試みる扱いのため、Acroで止まらない原因は `RangeFinder -> Proximity -> Avoidance` のどこかで途切れている可能性が高い。
- 走行テストを繰り返す前に、静置またはタイヤ浮かせ状態でProximity Viewer / PRXログの確認へ戻る。

次の最優先:

```text
20260612_02_lidar_proximity_static_check
```

確認項目:

- `RangeFinder1(cm)` が0.5m / 1m / 2mで正しく変化する。
- Mission PlannerのProximity Viewerで前方障害物が見える。
- DataFlashログに `PRX` 系メッセージが出る。
- `PRX1_TYPE=4`、`AVOID_ENABLE=7`、`AVOID_MARGIN=2.0`、`AVOID_BACKUP_SPD=0` が再起動後も残っている。
- `RNGFND1_ORIENT=0` とLiDAR搭載向きが一致している。
- `RCx_OPTION=40` が割り当てられている場合、Proximity Avoidance ON側になっている。

## 2026-06-12 LiDAR Proximity静置切り分け

詳細記録:

```text
logs/test_runs/20260612_02_lidar_proximity_static_check.md
```

追加確認:

```text
PRX1_TYPE = 0
→ RangeFinder距離表示あり

PRX1_TYPE = 4
→ 距離表示なし
```

判断:

- Acroで2m停止しなかった直接原因は、実走時点で `PRX1_TYPE=0` だったことが大きい。
- ただし、`PRX1_TYPE=4` にすると距離表示が消えるため、そのまま実走再試験には進まない。
- `PRX1_TYPE=4` でRangeFinder表示が消えても、Proximity ViewerまたはDataFlashの `PRX` に前方障害物が出るかを確認する。
- Proximity Viewer / `PRX` のどちらにも出ない場合は、`PRX1_TYPE=0` に戻し、RangeFinder単体またはGCS側Auto-stop方針へ戻す。

次の最優先:

```text
PRX1_TYPE=4 の状態で、Proximity ViewerまたはPRXログに前方障害物が出るか確認する。
```

### Navigation / Auto

| パラメータ | 現在値 | 次の判断 |
| --- | --- | --- |
| `WP_SPEED` | `2` | 初回Auto/Guided前に低速化検討 |
| `WP_RADIUS` | `2` | 狭すぎないか確認 |
| `TURN_RADIUS` | `0.9` | 実測最小旋回半径と照合 |
| `RNGFND1_MAX_CM` | `700` | Auto-stop閾値とGCS表示で判断 |
