# 配線

## 記録方針

ハードウェア変更の前後で、実際の配線を記録します。必要に応じて `photos/wiring/` に写真を置き、`diagrams/` に配線図を保存します。

## ポート割り当て

| 機器 / 信号 | FCポート | ケーブル / コネクタ | 電源 | メモ |
| --- | --- | --- | --- | --- |
| 受信機 | RC / SBUS系 | RadioLink R8EF の `S.BUS / CH1` | FC側 | `photos/wiring/20260608_現行配線_受信機_RadioLinkR8EF_SBUS接続.JPG` で確認。受信機は RadioLink R8EF V1.6。 |
| ステアリングサーボ | MAIN OUT 1 | 3線サーボケーブル | サーボレール | 確認済み。`SERVO1_FUNCTION=26`。換装後に向きを再確認。 |
| ESC / スロットル | MAIN OUT 3 | 3線サーボケーブル | ESC / サーボレール | 確認済み。`SERVO3_FUNCTION=70`。換装後にESCキャリブレーションと向きを再確認。 |
| GPS | GPSポート | M8N GPS系ケーブル | FC 5V | `photos/wiring/20260608_現行配線_GPSポート_M8N接続.JPG` でGPSポート接続を確認。ピン順は6C mini変換前に要確認。 |
| コンパス | I2Cポート | M8N Compass I2C系ケーブル | FC 5V | `photos/wiring/20260608_現行配線_I2Cポート_コンパス接続.JPG` でI2Cポート接続を確認。SDA/SCL/5V/GNDのピン順は要確認。 |
| テレメトリ | TELEM2 | UART | FC 5V | 現行Pixhawk 2.4.8Proでは`TELEM2`接続。6C Mini換装後はRaspberry Pi / MAVLinkを`TELEM1`へ移す候補。 |
| コンパニオンコンピュータ | TELEM2 | UART | Pi側電源は別系統 | `rover-gcs`資料では Raspberry Pi Zero 2 WH / Rpanion 構成。現行TELEM2から6C Mini `TELEM1`へ移す候補。 |
| LiDAR / RangeFinder | SERIAL4/5系 | UART | FC 5V | 現行パラメータでは `SERIAL4_PROTOCOL=9`, `SERIAL4_BAUD=115`。Pixhawk 6C Miniでは`SERIAL4`が物理`GPS2`になるため、丸コピーせずTELEM系へ移す。 |
| Safety Button | SWITCH | 2線/専用ケーブル | FC | 削除予定。Pixhawk 6C mini換装後は使わず、ESC電源物理カットで代替。 |
| Buzzer | BUZZER | 2線/専用ケーブル | FC | 削除予定。Pixhawk 6C mini換装後はGCS/PC通知で代替。 |
| USB | USB | USBケーブル | PC / FC | 削除予定。換装後の常用接続はMAVLinkテレメトリ経由を基本にする。 |

## 電源系統

| 電源元 | 供給先 | 電圧 | 電流容量 | メモ |
| --- | --- | --- | --- | --- |
| バッテリー | | | | |
| BEC | | | | |
| PM02 | Pixhawk 6C Mini `POWER` | 5V出力 + 電圧/電流検出 | 未確認 | Pixhawk 6C Miniとセットで購入したパワーモジュールを使用。旧Power Moduleは流用しない。 |

## 信号方向チェック

- [ ] ステアリング出力が意図した方向に動く
- [ ] スロットル出力が意図した方向に動く
- [ ] RC入力チャンネルが正しく割り当てられている
- [ ] モードスイッチ位置とモードが一致している
- [ ] タイヤを浮かせた状態でフェイルセーフを確認した

## 写真参照

| 日付 | 写真パス | 写っているもの | メモ |
| --- | --- | --- | --- |
| 2026-06-08 | `photos/wiring/20260608_現行配線_Pixhawk出力側_受信機周辺.JPG` | Pixhawk出力ピン側、受信機周辺、サーボ/ESC系の配線 | MAIN OUT側の接続状態を確認するための写真。ピン番号とケーブル行き先は別途ラベル確認が必要。 |
| 2026-06-08 | `photos/wiring/20260608_現行配線_Pixhawk上面ポート接続.JPG` | Pixhawk 2.4.8Pro上面、POWER、TELEM、GPS、I2C、SWITCH、BUZZER付近 | 旧FC撤去前のポート接続状態。6C miniへの変換表を作る元資料。 |
| 2026-06-08 | `photos/wiring/20260608_現行配線_TELEM2_コンパニオン接続.JPG` | Pixhawk TELEM2接続、コンパニオンコンピュータ側と思われる配線 | TELEM2を6C mini側のテレメトリポートへ移すための近接写真。 |
| 2026-06-08 | `photos/wiring/20260608_現行配線_受信機_RadioLinkR8EF_SBUS接続.JPG` | RadioLink R8EF V1.6 受信機、`S.BUS / CH1` 接続 | 受信機種別とSBUS接続を確認する写真。 |
| 2026-06-08 | `photos/wiring/20260608_現行配線_GPSポート_M8N接続.JPG` | Pixhawk GPSポート、M8N GPS配線、GPS周辺コネクタ | GPS UART系を6C miniへ移すための近接写真。 |
| 2026-06-08 | `photos/wiring/20260608_現行配線_I2Cポート_コンパス接続.JPG` | Pixhawk I2Cポート、M8N Compass I2C系配線 | Compass I2C系を6C miniへ移すための近接写真。 |

## 写真から確認できること

- 現行FCは Pixhawk 2.4.8Pro / Pixhawk Pro 系の上面ポート構成。
- `POWER`、`TELEM`系、`GPS`、`I2C`、`SWITCH`、`BUZZER` 付近に接続がある。
- `MAIN OUT 1` にステアリングサーボ、`MAIN OUT 3` にESC / スロットルが接続されている。
- 受信機は RadioLink R8EF V1.6 で、`S.BUS / CH1` に接続されている。
- コンパニオンコンピュータ系は現行TELEM2接続。6C Mini換装後は`TELEM1`へ移す候補。
- M8N GPSはGPSポート、Compass系はI2Cポートへ分かれて接続されている。
- LiDAR / RangeFinderは現行パラメータ上 `SERIAL4_PROTOCOL=9`, `SERIAL4_BAUD=115` のため、現行FCのSERIAL4系に接続されている扱い。
- Pixhawk 6C miniへ換装する前に、コネクタが刺さるかではなく、各信号のピン順を実測・目視で確認する必要がある。

## 換装先Pixhawk 6C Miniの写真確認

| 日付 | 写真パス | 写っているもの | メモ |
| --- | --- | --- | --- |
| 2026-06-08 | `photos/fc_mount/20260608_換装先FC_Pixhawk6Cmini_上面.JPG` | Pixhawk 6C Mini上面、矢印、`AUX5`、`AUX6`、`RC IN` | Holybro公式の Pixhawk 6C Mini Model A (Current) のポート図と一致。 |
| 2026-06-08 | `photos/fc_mount/20260608_換装先FC_Pixhawk6Cmini_POWER_TELEM_GPS_CAN側.JPG` | `POWER`、`TELEM1`、`TELEM2`、`GPS1`、`CAN1`、`CAN2` | 電源、GPS、テレメトリ接続の移行先候補。 |
| 2026-06-08 | `photos/fc_mount/20260608_換装先FC_Pixhawk6Cmini_MAIN_AUX_RCIN側.JPG` | `MAIN 1-8`、`AUX 1-4`、`AUX5`、`AUX6`、`RC IN` | ステアリングは`MAIN 1`、ESCは`MAIN 3`へ移す前提。 |
| 2026-06-08 | `photos/fc_mount/20260608_換装先FC_Pixhawk6Cmini_DSM_RSSI_RCIN側.JPG` | `DSM`、`RSSI`、`RC IN` | RadioLink R8EFのSBUSは`RC IN`へ移す候補。 |
| 2026-06-08 | `photos/fc_mount/20260608_換装先FC_Pixhawk6Cmini_USB_FMUDebug_GPS2_I2C側.JPG` | USB-C、`FMU Debug`、`GPS2`、`I2C` | `GPS2`は6ピン対応。現行M8NのGPS UART + Compass I2Cは、まず`GPS2`へまとめる方針。 |

## 換装時の写真確認方針

- 裏面に型番、Model、Rev、シリアル等の追加表記はない。
- 箱はないため、箱ラベルによる追加確認はしない。
- 現行FC側の追加接写は、既存写真に含まれる情報で進める。
- 物理的に撮影が難しい箇所は、無理に撮影せず、抜線時の実機目視で補足する。

## 旧FCからPixhawk 6C Miniへの変換表 初版

まだ接続しない。次に、公式ピン表と実ケーブルの線色を照合してから接続する。

| 現行Pixhawk 2.4.8Pro側 | 現行用途 | Pixhawk 6C Mini側候補 | 状態 | メモ |
| --- | --- | --- | --- | --- |
| `POWER` | 電源モジュール / 電圧電流計測 | PM02 -> `POWER` | 使用予定 | Pixhawk 6C Miniとセットで購入したPM02を使用。旧Power Moduleは流用しない。接続後にMission Plannerで電圧/電流表示を確認し、`BATT_*`を再設定する。 |
| `TELEM2` | Raspberry Pi / MAVLinkテレメトリ | `TELEM1` | 優先候補 | 現行はTELEM2だが、6C Miniでは`TELEM2`をLiDARへ使うため、Raspberry Piは`TELEM1`へ移す。対応する`SERIAL1_PROTOCOL=2`、`SERIAL1_BAUD=921`を候補にする。 |
| `GPS` + `I2C` | M8N GPS UART + Compass I2C | `GPS2` 6ピン | 優先候補 | 6C Miniの`GPS2`は6ピン対応。`VCC / TX / RX / SCL / SDA / GND`にまとめる。現行M8Nの2股を6ピン変換ケーブルへ集約する方針。 |
| `GPS` | M8N GPS UART | `GPS1` | 予備候補 | `GPS1`は10ピンでSafety/Buzzer系も含むため、今回は優先しない。 |
| `I2C` | M8N Compass I2C | `I2C` | 予備候補 | `GPS2`へまとめられない場合の予備候補。`5V / GND / SCL / SDA` の順番を必ず確認する。 |
| `I2C` | OLED Module | `I2C` | 確認済み | OLED基板印字は `GND / VCC / SCL / SDA`。6C Mini独立`I2C`へ変換し、`NTF_DISPLAY_TYPE=1`で表示成功。 |
| `SERIAL4/5` | LiDAR / RangeFinder | `TELEM2` | 確認済み | 6C Miniでは`SERIAL4`が`GPS2`に対応し、GPS用に使うためLiDARには使わない。LiDARは`TELEM2`へ移し、`SERIAL2_PROTOCOL=9`、`SERIAL2_BAUD=115`でMission Planner上のSonar Range表示を確認済み。 |
| RC / SBUS系 | RadioLink R8EF SBUS | `RC IN` | 候補 | 受信機の`S.BUS / CH1`から6C Miniの`RC IN`へ移行する。`S / + / -` の向きを確認する。 |
| `MAIN OUT 1` | ステアリングサーボ | `MAIN 1` | 確定候補 | 現行`SERVO1_FUNCTION=26`。換装後にステアリング方向をベンチ確認する。 |
| `MAIN OUT 3` | ESC / スロットル | `MAIN 3` | 確定候補 | 現行`SERVO3_FUNCTION=70`。換装後にESCキャリブレーションとニュートラルを確認する。 |
| `SWITCH` | Safety Button | 接続しない | 削除予定 | ESC/モーター電源の物理カット、RCスロットルカット、`ARMING_CHECK`で代替する。 |
| `BUZZER` | Buzzer | 接続しない | 削除予定 | rover-gcs / PC側の警告表示と音通知で代替する。 |
| `USB` | 常用USB接続 | 常用接続しない | 削除予定 | 設定作業、ファーム書き込み、緊急作業時のみ必要に応じて使う。 |

## GPS2 6ピン変換メモ

現行M8Nの2股配線を、Pixhawk 6C Miniの`GPS2` 6ピンへまとめる。

| GPS2側信号 | 接続する現行M8N側 | 注意 |
| --- | --- | --- |
| `VCC` | GPS UART系またはCompass I2C系の5V | 5Vは共通化する。重複給電にならないよう配線を整理する。 |
| `UART TX` | M8N GPS側 `RX` | FCからGPSへ送る線。TX/RXは交差接続。 |
| `UART RX` | M8N GPS側 `TX` | GPSからFCへ送る線。TX/RXは交差接続。 |
| `I2C SCL` | M8N Compass側 `SCL` | SCL同士を接続。 |
| `I2C SDA` | M8N Compass側 `SDA` | SDA同士を接続。 |
| `GND` | GPS UART系またはCompass I2C系のGND | GNDは共通化する。 |

`GPS2` 6ピンへまとめられない場合だけ、`GPS1`または独立`I2C`ポートを予備候補として使う。

## OLED Module I2C実験メモ

現行Pixhawk Proセット付属のOLED Moduleを、Pixhawk 6C Miniの独立`I2C`ポートへ合わせる実験を行う。

2026-06-08: ハンダ後に6C Mini独立`I2C`へ接続し、`NTF_DISPLAY_TYPE=1`を設定して再起動したところ表示成功。

写真上のOLED基板印字:

```text
GND
VCC
SCL
SDA
```

写真上の線色:

```text
GND = 黒
VCC = 赤
SCL = 緑
SDA = 黄
```

Pixhawk 6C Mini独立`I2C`ポート:

```text
Pin1 VCC
Pin2 I2C2_SCL
Pin3 I2C2_SDA
Pin4 GND
```

接続対応:

| OLED Module | Pixhawk 6C Mini `I2C` | 注意 |
| --- | --- | --- |
| `GND` / 黒 | Pin4 `GND` | 導通で確認する。 |
| `VCC` / 赤 | Pin1 `VCC` | 5V給電。 |
| `SCL` / 緑 | Pin2 `I2C2_SCL` | SCL同士。 |
| `SDA` / 黄 | Pin3 `I2C2_SDA` | SDA同士。 |

実験手順:

1. FC電源OFF。
2. OLED Module単体で接続する。GPS/CompassのI2C接続は一旦外しておく。
3. 6C Mini側はJST-GH 4Pの正規ケーブルまたはピグテールを使う。
4. OLED側の`GND / VCC / SCL / SDA`印字に合わせて結線する。
5. 通電前に`VCC-GND`がショートしていないことをテスターで確認する。
6. 通電後、OLEDが点灯するか確認する。
7. Mission Plannerで`NTF_DISPLAY_TYPE`を設定して再起動する。
   - SSD1306なら`1`
   - SH1106なら`2`
8. 表示が出ない場合は、`NTF_DISPLAY_TYPE`をもう一方に変えて再起動する。

注意:

- 既存の旧Pixhawk用4Pコネクタを6C Miniへ無理に刺さない。
- I2Cは複数デバイスを同じバスにぶら下げられるが、初回確認はOLED単体で行う。
- OLED確認後、Compassも同じI2Cに接続する場合は、アドレス衝突や接触不良を切り分ける。

## LiDAR / RangeFinder移行メモ

現行はLiDAR / RangeFinderが`SERIAL4/5`系に接続されている。パラメータ上は以下を現行値として扱う。

| パラメータ | 現行値 | 意味 |
| --- | --- | --- |
| `SERIAL4_PROTOCOL` | `9` | RangeFinder |
| `SERIAL4_BAUD` | `115` | 115200bps |
| `SERIAL5_PROTOCOL` | `-1` | 無効 |
| `RNGFND1_TYPE` | `20` | TF-Luna / RangeFinder系として扱う |
| `RNGFND1_MIN_CM` | `20` | 現行最小距離 |
| `RNGFND1_MAX_CM` | `200` | 換装前の現行値。rover-gcsのAuto-stop最大閾値`100cm`を十分カバーするため、6C Miniでも踏襲候補 |

Pixhawk 6C MiniではArduPilot上の`SERIAL4`が物理`GPS2`に対応する。今回は`GPS2`を現行M8N GPS + Compass用に使うため、LiDARへ`SERIAL4`設定を丸コピーしない。

推奨割り当て:

| 機器 | 6C Mini物理ポート | ArduPilot Serial候補 | 設定候補 | メモ |
| --- | --- | --- | --- | --- |
| Raspberry Pi / MAVLink | `TELEM1` | `SERIAL1` | `SERIAL1_PROTOCOL=2`, `SERIAL1_BAUD=921` | 現行TELEM2相当のMAVLink接続をTELEM1へ移す候補。 |
| LiDAR / RangeFinder | `TELEM2` | `SERIAL2` | `SERIAL2_PROTOCOL=9`, `SERIAL2_BAUD=115` | `TELEM2`でよい。Raspberry Piと同じポートには接続しない。 |
| M8N GPS + Compass | `GPS2` | `SERIAL4` | `SERIAL4_PROTOCOL=5` | `GPS2` 6ピンへGPS UART + I2Cをまとめる。 |

`TELEM2`をRaspberry Piで使い続ける場合は、LiDARを`TELEM1`へ接続して、`SERIAL1_PROTOCOL=9`、`SERIAL1_BAUD=115`にする。どちらでもよいが、1つのUARTポートにRaspberry PiとLiDARを同時接続しない。

LiDAR UARTの基本配線:

```text
6C Mini TELEMx TX -> LiDAR RX
6C Mini TELEMx RX -> LiDAR TX
6C Mini TELEMx GND -> LiDAR GND
5V給電はLiDARの仕様と消費電流を確認してから接続する
```

### TF-Luna -> 6C Mini TELEM2 6ピン作成メモ

TF-Luna公式ユーザーマニュアルのUART時ピン:

| TF-Luna No. | 機能 | UART時の扱い |
| --- | --- | --- |
| 1 | `+5V` | 電源 |
| 2 | `RXD / SDA` | UART受信 |
| 3 | `TXD / SCL` | UART送信 |
| 4 | `GND` | GND |
| 5 | Configuration Input | UARTでは未接続、または3.3V。GNDへ落とすとI2Cモードになるため接続しない。 |
| 6 | Multiplexing Output | 今回は未使用。 |

線色について:

- `TF-Luna User Manual.pdf` の `6.1 Description about Line Sequence and Connection` / `Table 6` は、ピン番号と機能を定義しており、線色は定義していない。
- BenewakeのQuick Implementation系資料には `1=Red/+5V`, `2=White/RXD-SDA`, `3=Green/TXD-SCL`, `4=Black/GND` の色表記がある。
- ただし、実機作業では線色を主根拠にせず、TF-Lunaコネクタのピン番号と導通確認を優先する。
- 今回の現物ケーブルは公式例と色が異なるため、以下の現物色を使って6C Mini側へ変換する。
- 最新の接写写真では、TF-Lunaコネクタを左から `赤 / 青 / 黄 / 緑 / 白 / 黒` と目視判別できる。
- ただし、最終結線では色だけでなく、TF-Lunaコネクタのピン番号と導通確認を優先する。

現状のTF-Lunaケーブル色:

TF-Luna本体を正面、レンズを上、コネクタを手前、線を下に見た状態で、左から以下の並びとする。

```text
No.1 = 赤 = +5V
No.2 = 青 = RXD / SDA
No.3 = 黄 = TXD / SCL
No.4 = 緑 = GND
No.5 = 白 = Configuration Input
No.6 = 黒 = Multiplexing Output
```

UART運用で使う線:

```text
赤 = +5V
青 = RXD
黄 = TXD
緑 = GND
白 = 未接続
黒 = 未接続
```

Pixhawk 6C Mini `TELEM2` 6ピン:

| 6C Mini `TELEM2` Pin | 機能 | TF-Luna側 | 接続 |
| --- | --- | --- | --- |
| Pin1 | `VCC +5V` | No.1 赤 `+5V` | 接続 |
| Pin2 | `UART5_TX` | No.2 青 `RXD` | 接続。FC TX -> LiDAR RX |
| Pin3 | `UART5_RX` | No.3 黄 `TXD` | 接続。FC RX <- LiDAR TX |
| Pin4 | `UART5_CTS` | - | 未接続 |
| Pin5 | `UART5_RTS` | - | 未接続 |
| Pin6 | `GND` | No.4 緑 `GND` | 接続 |

作成する6ピンコネクタ:

```text
6C Mini TELEM2 Pin1 VCC  -> TF-Luna 赤 +5V
6C Mini TELEM2 Pin2 TX   -> TF-Luna 青 RXD
6C Mini TELEM2 Pin3 RX   -> TF-Luna 黄 TXD
6C Mini TELEM2 Pin4 CTS  -> 未接続
6C Mini TELEM2 Pin5 RTS  -> 未接続
6C Mini TELEM2 Pin6 GND  -> TF-Luna 緑 GND
```

6C Mini側JST-GH 6ピンの推奨線色:

```text
Pin1 VCC  = 赤
Pin2 TX   = 青
Pin3 RX   = 黄
Pin4 CTS  = 未使用
Pin5 RTS  = 未使用
Pin6 GND  = 緑、または黒
```

理由:

- `Pin2 TX`はTF-Lunaの青 `RXD`へ接続するため、6C Mini側も青にすると追いやすい。
- `Pin3 RX`はTF-Lunaの黄 `TXD`から受けるため、6C Mini側も黄にすると追いやすい。
- TF-Luna現物では緑が`GND`なので、緑をTX/RXへ使うと混乱しやすい。
- 黒線を使えるなら、GNDは黒にするのが一般的。TF-Luna側の緑GNDへ接続する。

注意:

- TF-Lunaの5番Configuration Input、現物色では白線はGNDへ接続しない。GNDへ落とすとI2Cモードになる。
- TF-Lunaの6番Multiplexing Output、現物色では黒線は今回未使用。
- TF-LunaはUART既定で115200bps。
- 6C Mini側は`TELEM2`を使う場合、ArduPilot上は`SERIAL2`。
- `SERIAL2_PROTOCOL=9`, `SERIAL2_BAUD=115`を候補にする。
- 2026-06-08: Mission Plannerで `Sonar Range (m) = 1.50` の表示を確認。LiDAR / RangeFinderのTELEM2接続は動作確認OK。

## 次にやること

1. 現行配線の一時ラベル貼りは省略する。
   - 代わりに、写真、ポート名、線色、実機目視で記録する。
   - 追加接写は省略し、既存写真と抜線時の目視で補足する。
2. `MAIN OUT` の確定情報を換装表に反映する。
   - ステアリングサーボ: `MAIN OUT 1`
   - ESC / スロットル: `MAIN OUT 3`
3. 現行M8N GPSの2股を分けて確認する。
   - GPS UART: `5V / GND / TX / RX`
   - Compass I2C: `5V / GND / SDA / SCL`
   - 6C Mini側は`GPS2` 6ピンへまとめる方針。
   - `GPS2`側信号: `VCC / UART TX / UART RX / I2C SCL / I2C SDA / GND`
4. TELEM系の移行先を確定する。
   - 推奨: Raspberry Pi / MAVLinkを`TELEM1`へ移す。
   - 推奨: LiDAR / RangeFinderを`TELEM2`へ移す。
   - 理由: `GPS2`はM8N GPS + Compass用に使うため、6C Mini側の`SERIAL4`はLiDARに使わない。
5. Pixhawk 6C Mini側のポート表と照合し、変換表を作る。
   - 旧Pixhawk 2.4.8Proポート
   - 信号名
   - 線色
   - Pixhawk 6C Mini側ポート
   - 6C Mini側ピン番号
   - PM02はPixhawk 6C Miniセット付属品を使用するため、旧Power Moduleの流用変換は行わない。
6. Safety Button、Buzzer、USBは削除予定として扱う。
   - Safety Button: 6C Miniへ移植しない。
   - Buzzer: 6C Miniへ移植しない。
   - USB: 常用配線から外す。設定・緊急作業時のみ必要に応じて使う。
7. Safety Button / Buzzer削除後の代替安全策を確定する。
   - ESC/モーター電源の物理カット
   - RCスロットルカット
   - `ARMING_CHECK` の安全側設定
   - GCS/PC側の警告表示と音通知
8. 追加撮影は省略し、配線変換表の作成に進む。
   - 現行FC側の近接写真は既存写真で代替する。
   - 物理的に見えない箇所は、抜線時に線色と行き先を目視で補足する。
   - 次の作業は、旧FCポートとPixhawk 6C Miniポートの対応表作成。

## 図面参照

| 日付 | 図面パス | 対象範囲 | メモ |
| --- | --- | --- | --- |
| | | | |
